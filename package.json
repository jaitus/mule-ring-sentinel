{
  "name": "mule-ring-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Defense-only detector for merchants used as money-mule conduits on payment-aggregator rails. Razorpay AI Buildathon Track 2 entry.",
  "scripts": {
    "slice": "node src/eval/quickEval.js",
    "eval": "node src/eval/report.js",
    "pipeline": "node src/pipeline/run.js",
    "dev": "node src/server/api.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
